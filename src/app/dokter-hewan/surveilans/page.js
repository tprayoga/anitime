import { SurveilansView } from 'src/sections/dokter-hewan/surveilans/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Surveilans',
};

export default function DashboardViewPage() {
  return <SurveilansView />;
}
