import { TransaksiView } from 'src/sections/finance/transaksi/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Transaksi',
};

export default function TransaksiViewPage() {
  return <TransaksiView />;
}
